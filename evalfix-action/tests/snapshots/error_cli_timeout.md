<!-- evalfix-action -->
## EvalFix — setup needs attention ⚠️

### EvalFix timed out

Increase `timeout_minutes` or reduce the number of cases, then re-run the workflow.

Last CLI output:

```text
evaluation exceeded the deadline
```

If this persists, [open an issue](https://github.com/evalfix/evalfix-action/issues/new) with the workflow logs.
