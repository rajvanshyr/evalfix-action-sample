<!-- evalfix-action -->
## EvalFix — setup needs attention ⚠️

### Agent eval files were not found

EvalFix expected this layout at `agents/support`:

```text
agents/support/
├── prompt.txt
└── evals.yaml
```

Create the files with `evalfix init agents/support` and push them to the PR. [Read the setup docs](https://github.com/evalfix/evalfix-action#readme)
